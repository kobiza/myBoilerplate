define(['react', 'lodash', 'components/myApp.rt'], function (React, _, template) {
    'use strict';

    return React.createClass({
        displayName: 'myApp',
        render: template
    });
});
